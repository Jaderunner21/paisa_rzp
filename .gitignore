__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
reports/

# Credentials. .env.example is committed; .env never is.
.env

# Cached model adjudications, keyed by provider + model + prompt. Machine-local
# and never shared: a cached answer is evidence about one run on one machine,
# and committing it would let one person's result silently become everyone's.
.cache/
